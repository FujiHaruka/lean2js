# 保証の鎖を閉じる

`docs/binding-shapes-plan.md` が断片を 33/35 形・公開関数 58/67 本まで広げたあとに残った穴を、
上から下まで全部塞ぐための計画。

## 文脈

**この計画は完遂した。** 塞いだのは 5 つの穴で、いま受け持っているのはすべて証明の側。

| 穴 | いま受け持っているもの |
| --- | --- |
| `call` / `fnRef` と、それを通る公開関数 9 本 | 証明（断片が `Core.Expr` の 35 形すべてを覆う） |
| `noMatchingAlternative` を写せない | 証明（`Exhaustive.lean` の全域な usefulness 検査） |
| `outOfFuel` を写せない | 証明（`Cost.lean`）と、`emit` がプログラムごとに 1 回見る `cost` |
| AST からファイルのテキストまで | 証明（`Roundtrip.lean` / `HelperAgree.lean`） |
| `.d.ts`（利用者が実際に呼ぶ境界） | 証明（`Dts.lean`）—— 成り立つのは片方向 |

上から 3 つは主張の但し書き、下から 2 つは TCB の厚み。前者は「何を言えているか」、後者は
「どこまで言えているか」を決めている。

## ゴール

このマイルストーンの終わりに立っている場所。

- 出荷する公開関数 **67 本すべて**について、どんな引数でも、`eval` が値を返すなら生成モジュールの
  同名関数が同じ値を返し、`eval` が落ちるなら同じコードで落ちる。**但し書きは 1 つも付かない**
- `Agree` の実行時検査は回帰テストとして残るが、主張の裏付けではなくなる
- 保証の鎖が「Lean のリファレンス意味論 → JS の模型 → 出荷するファイルのテキスト」まで繋がり、
  TCB は「パーサの仕様と Node」に縮む
- `.d.ts` が約束する型を満たす呼び出しは、入口の実行時検査で弾かれない

## 非目標

これまでの判断を引き継ぐもの（Promise / async、クロージャ、公開境界に関数を出すこと、`sortBy`、
Float）に加えて、このマイルストーンで新たに外すもの。

| 外すもの | 理由 |
| --- | --- |
| JS 全体の字句・構文 | パーサが受けるのは**この処理系が出力する部分集合**だけでよい。JS を読む道具を作るのではなく、書いたものを読み返して同じ木に戻ることを言う |
| TypeScript の型検査器のモデル化 | `.d.ts` の健全性は「型を満たす値の集合」として述べれば閉じる。代入互換性の規則そのものは要らない |
| Node / V8 の正しさ | 模型と本物の JS の差は差分テストが受け持つ。ここは動かさない |
| 意味論に新しい概念を足すこと | 言語は凍ったまま。分母は 1 つも増やさない |

## Approach

原則が四つあり、Step の順序と刻み方はこの四つから出る。

**帰納の軸を先に揃える。** `fragment_correct_in` と `fragment_traps_in` は `InFragment` の**導出**に
ついての帰納になっている。呼び出しの本体は呼び出し式の部分項ではないので、この形のままでは `call` の
場合に帰納法の仮定が出ない。`typeSound` と同じく**燃料についての帰納**にし、断片は場合分けとして
使う。`eval` はどの形でも部分式を 1 段少ない燃料で評価するので、燃料の仮定は部分式にも呼び出し先の
本体にも同じ強さで効く。**この付け替えは覆う範囲を 1 ミリも動かさないコミットとして先に済ませる。**

**プログラム全体の不変条件は成果物から取り出す。** `call` を覆う主張は「呼び出し先の本体も型検査を
通る」を要る。これはプログラム全体についての性質だが、利用者に渡させるものではない ——
`compileProgram` が成功したことから取り出せる（`ProgramTyped`）。利用者から見える主張に但し書きは
増やさない。

**地面を掘るのは主張を閉じたあと。** テキストと `.d.ts`（Step D / E）は、いま証明が立っている場所の
下と横に地面を足す作業で、主張の文面（Step A–C）とは独立に進む。ただし順序は主張を先にする ——
但し書きが残ったまま地面を広げると、README の保証の組み立てを 2 回書き直すことになる。

**保証を弱めて緑にしない。** 証明が重いときに命題を弱めない。`sorry` で塞がない。実行時検査が
落ちたときにベクタを減らさない。これはこのリポジトリの不変条件で、このマイルストーンでも変わらない。

## Step A. 呼び出しを断片に入れる — 完了

**動いた数字**: 断片 33/35 → **35/35 形**、陽性・陰性が届く公開関数 58/67 → **67/67 本**、manifest の
`Claim` 12 → 13 本。生成した JS は 1 バイトも動いていない。

**帰納の軸は燃料になった。** `fragment_correct_in` / `fragment_traps_in` は `InFragment` の導出に
ついての帰納だったが、呼び出しの本体は呼び出し式の部分項ではない。`eval` はどの形でも部分式を 1 段
少ない燃料で評価するので、燃料についての帰納にすると部分式にも呼び出し先の本体にも同じ強さで効く。

**宣言単位の主張と同時に立つ。** 呼び出しの場合が要るのは「呼ばれた宣言について、その燃料で生成関数が
同じ値を返す」で、それは宣言単位の主張そのもの。式の主張（`AgreesAt` / `TrapsAt`）と宣言の主張
（`DeclAgrees` / `DeclTraps`）を 1 つの帰納で同時に上げる（`Decl.levels`）。入れ子の `let` は 1 つに
つき燃料を 1 使うので、帰納は「その燃料以下のすべて」を運ぶ。

**前置プログラムとの差が消えた。** 宣言は**全体プログラムに対して**コンパイルされるようになり、
「呼び出しは自分より前の宣言にしか行けない」は `callsPrecede` の明示の検査になった。コンパイラの
2 つのパスを突き合わせる補題（`compileExpr_types_irrel` の一族、275 行）はまるごと不要になった。

**途中で見つかったコンパイラの穴を 2 つ塞いだ。** どちらも証明が通らないことで見つかった。

| 穴 | 直し方 |
| --- | --- |
| `@f` が同名の束縛に影を作られると、`eval` は宣言を読み、生成 JS は束縛を読む | 束縛と衝突する関数参照をコンパイルエラーにした（呼び出しは以前から同じ検査を持っていた） |
| `.tag` の射影は判別子と衝突するが、拒んでいたのは型宣言時の検査だけだった | 射影そのものを拒むようにした。`InFragment` から但し書きが消え、断片が構文そのものになった |

**環境の不変条件が 2 つ増えた。** どちらも呼び出しが環境から行き先を読むために要る ——
参照環境が束縛する名前は文脈にもあること（`EnvTyped.inScope`）と、生成環境が持つ予約されていない
名前は参照環境も持つこと（`JsEnvAgrees.fresh`）。**利用者から見える主張に但し書きは増えていない。**

**保証の境界**: 動いた。README の「保証の組み立て」と `docs/mvp-plan.md` の到達点は同じコミットで
直してある。

## Step B. 網羅性検査を証明する — 完了

**動いたもの**: `Mirrorable` から `noMatchingAlternative` が外れた。trap 方向が除くのは `outOfFuel`
だけになった。

**`useful` は全域になった。** 降りる深さの上限（`usefulBudget`）を引数に取り、1 段ごとに減らす。
測度で悩まずに済み、`partial` が外れる。**使い切ったときは「useful」と答える** —— 網羅性検査は
そこで「網羅でない」とコンパイルエラーになり、通してしまう側には倒れない。出荷プログラムは今の
上限で通る（ゲートで確認済み）。

**健全性は `Exhaustive.lean`。** 「useful でない行列は、その列の型が取りうるすべての値ベクタを覆う」。
中身は行列の分割を値の側から読み直す 2 本 —— `specialize` した行列で覆えるなら元の行列でも覆える、
`defaultRows` でも同じ —— と、「型が付いた値はシグネチャの見出しのどれかを持つ」。

**`specialize` が幅を見るようになった。** 構成子パターンの引数の本数がその見出しの本数と違う行は、
そのまま並べるのではなく落とす。型検査を通ったパターンでは起こらないので受理する集合は変わらず、
落とす方向は「useful」に倒れるので安全側。

**保証の境界**: 動いた。README の「保証の組み立て」と `docs/mvp-plan.md` の到達点は同じコミットで
直してある。

## Step C. 燃料の但し書きを消す — 完了

**到達点**: 出荷する主張から「燃料切れを除く」が消えた。`decl_traps_at_cost` は `Correct.Mirrorable`
を取らず、代わりに**プログラムについての 2 つの検査**を取る —— `Cost.progOk p` と
`Cost.cost p ≤ defaultFuel`。どちらも構文だけで決まるので `emit` がプログラムごとに 1 回確かめる
（この例題は `cost = 482`、出荷時の燃料は 10000）。結果について外せなかった条件が、入力についてその場で
決まる条件に置き換わった。

**燃料が数えているのは式の入れ子の深さと呼び出しの段数だけ。** `evalArgs` / `evalMapItems` /
`evalFilterItems` / `evalFindItems` / `evalQuantItems` / `evalReduceItems` は**どれもリストの残りを
同じ燃料で**評価する（`Eval.lean:399-464`）ので、20000 要素の `map` は燃料 2、同じ長さの `reduce` は
燃料 5 で答えを返す。だから `cost` は構文だけで書ける —— 強正規化の証明は要らない。

- **C-1 燃料の単調性 — 完了。** `LeanTs/Fuel.lean`。`Refines a b` は「`a` が燃料切れでない限り
  `a = b`」で、燃料が変えうるのはそれだけなので、主張はすべてこの形。**帰納は 1 段ずつ**
  （`evalExpr_succ`）にして、`f ≤ g` は差についての帰納で出す
- **C-2 コスト関数 — 完了。** `LeanTs/Cost.lean`。上界は
  `cost p = maxBodyDepth p.decls + p.decls.length * (maxBodyDepth p.decls + 1)` ——
  1 段あたり最も深い本体ぶん、段数は宣言の本数まで。整礎再帰は要らない。
  **帰納は燃料についてで、式についてではない**（`Fuel.lean` と同じ形）: 呼び出しは 1 段少ない燃料で
  呼び先の本体に入るので、`evalExpr_succ` と同じ 1 段ずつの帰納で 35 形すべてが降りる。
  降りる先の宣言の位置 `j` が `i` より小さいことが、その 1 段で費やされる燃料を賄う。

  **関数値が絡む呼び出しは、構文の側で閉じてある。** `evalExpr` の `.call` は `calleeOf env fn` で
  呼び先を決めるので、環境に入った `.fn g` がどの宣言を指すかが分からないと段数が押さえられない。
  `progOk` が見ているのはそこ —— `fnRef` は呼び出しの引数にしか置けず、その位置のパラメータは関数型で
  なければならず、指す宣言は呼び先より前。関数型のパラメータ名は `.var` として読めない（呼ぶことしか
  できない）。この 3 つで「関数値は容器に入らない」が言えるので、環境に現れる `.fn g` は必ず
  「いま走っている宣言より前の宣言」を指す。**この検査はコンパイラの `wfTy` / `fnArgsPrecede` が
  課しているものと同じだが、`progOk` は構文から独立に見る** —— 燃料の主張がコンパイラの正しさに
  ぶら下がらないようにするため。

  **入口の引数も `noFn` でなければならない。** `evalCall` は `Value.hasTy` を通った引数しか本体に
  入れないので、関数値が入り込まないことは型から出る —— ただし「宣言された型が関数型を隠していない」
  ことが要る。だから `progOk` は `typesFirstOrder`（宣言された全フィールドの型に関数型が現れない）も
  見る。公開宣言（`isPublic`）はパラメータ自体が関数型でないので、これで入口は閉じる
- **C-3 主張から燃料を落とす — 完了。** `Decl.decl_traps_at_cost`。`decl_traps` はそのまま残る
  （`Mirrorable` を取る一般形）。`Example.lean` の `add_traps` / `cartTotal_traps` / `addMoney_traps`
  は前者を使い、`Correct.Mirrorable` の仮定が消えた。
  `callFunctionAt` 側の「十分な燃料で」は生成コードの模型の話なので残る
- **C-4 出荷時の 10000 — 完了。** `emit` が `progOk` と `cost ≤ defaultFuel` を書き出す前に確かめる。
  ベクタごとではなく**プログラムごとに 1 回**。README の「実行時検査」に残る燃料の役目は、
  模型の側の燃料だけになった

## Step D. 生成テキストまで届かせる — 完了

**目標**: TCB を「レンダラと手書き JS」から「パーサの仕様と Node」へ縮める。

いま証明は `Js.Module` の AST で止まっていて、出荷しているのはそれを `render` したテキスト。間に
証明が無い。

- **D-1 レンダラを全域に — 完了。** `Js.Expr.render` / `TyDesc.render` / `tsType` は
  リストごとの相互再帰に書き直してあり、`partial` は付いていない。出力は 1 バイトも動いていない
- **D-2 字句 — 完了。** 数値とエスケープは `toString` と `String.foldl` を捨て、`LeanTs/Text.lean` の
  `List Char` 再帰にした。どちらも proof が展開できない壁で、読み返す側の証明が書けない。出力は
  1 バイトも動いていない。読む側は `LeanTs/Parse.lean` の `parseInt` / `parseStr` / `parseIdent` で、
  それぞれ「書いたものの後ろに何が続いていても、書いたものと残りを返す」形の往復補題を持つ
- **D-3 型記述子 — 完了。** `parseDesc (render d ++ rest) = some (d, rest)`。ついでに
  `TyDesc.ctors` が持っていた型名を落とした —— `render` は書かず `checkTy` は読まないのに、
  `render` を単射でなくしていた
- **D-4 読む側 — 完了。** 式・文・関数・モジュールを読む `Parse.lean`。出荷する `index.js` が
  コンパイラの作った AST に読み戻せることを **`emit` が書き出す前に検査する** ——
  `checkAgreement` と同じ位置で、出荷するテキストそのもので毎回確かめる

  **印字器は出荷ファイルそのものを書くようになった。** `Js.Module.render` は誰も呼んでおらず、
  出荷していたのは `emitModule` が組み立てる別のテキストだった。往復を言う相手が出荷ファイルで
  ないと主張が空になるので、`render` を出荷ファイル（末尾の `//# sourceMappingURL` まで）に
  合わせ、`emitModule` がそれを使うようにした。出力は 1 バイトも動いていない

  パーサの再帰は燃料で回す。上限は入力テキストの長さから取るので、**主張に燃料の但し書きは
  出ない**（Step C が消そうとしている意味論の燃料とは別物）
- **D-5 往復の証明 — 完了。** `LeanTs/Roundtrip.lean` の `parseModule_render` ——
  `RenderableModule m` なら `parseModule (Js.Module.render m).toList = some m`。

  **`Renderable` が除くのは、`render` が同じテキストに写す 3 か所と、自分を閉じる doc。**

  | 重なるテキスト | 除き方 |
  | --- | --- |
  | `.call "__ck" [e, d]` は `.check d e` と同じ。`__map` ほか 6 つの構成子も同じ | 呼び先と `.ident` の名前が、読み手が構成子に振り分ける 10 語（`true` / `false` / `new` と `__ck` ほか 7 つ）でないこと。`__i53` のような**構成子を持たないヘルパ名は素通り**する |
  | `.ident "true"` は `.bool true` と、`.ident "new"` は `dictLit` の頭と同じ | 同上 |
  | `.unary op` の `op` が任意の文字列 | `op` が `!` か `-` であること |

  加えて `Func.doc` は `/** */` の中に素で入るので、`" */\n"` を含まないことが要る
  （`noCommentClose`）。名前はすべて識別子であること。

  **燃料の仮定はテキストの長さ**（`(render e).toList.length < f`）。最上位はテキスト自身の長さを
  渡すので、主張に燃料の但し書きは残らない。型記述子の読み手だけは測度を取るので、
  `descSize d ≤ (render d).length` の橋を 1 本渡している。

  **証明の要は、`(` の後の先読み `( 識別子並び ) => (` が arrowCall 以外の括弧形では必ず失敗する**
  こと（`parseArrowHead_render`）。識別子並びは名前に使えない最初の文字で止まり、その文字は
  どの `render` でも `)` にならない —— `member` は `).` で閉じ、`binary` と `cond` は空白が来る。
  ここは部分式への帰納ではなく、構成子ごとの場合分けだけで閉じる。

  **印字器から `String.intercalate` が消えた。** 累算器を回すので proof が展開できない。
  引数名の並びは `Js.renderNames`、文の並びは `Js.Stmt.renderAll` という素の再帰にした。
  前置きと末尾の source-map 行も `Js.preamble` / `Js.sourceMapLink` という名前を持つ ——
  `runtime` の 4000 字を展開させないため。出力は 1 バイトも動いていない
- **D-5b `Renderable` を成果物から取り出す — 完了。** `LeanTs/Renderable.lean` の
  `renderableModule_of_compileProgram` —— `compileProgram p = .ok m → RenderableModule m`。
  **往復の主張から但し書きが消えた**: `parseModule_render_of_compileProgram` は
  「コンパイルが通ったなら、書き出したテキストは同じモジュールに読み戻る」を無条件で言う。
  `Axioms.lean` がこの行を固定している。

  **証明はコンパイラ自身の再帰**（`compileExpr.mutual_induct` の 56 場合）。生成木に入る名前は
  2 か所からしか来ない —— スコープと宣言で、どちらも `validateIdent` を通っている。演算子と呼び先は
  コンパイラが自分で綴る一握りで、読み手が構成子に振り分ける 10 語のどれでもない。`__p0` や `__i53`
  のような予約接頭辞のヘルパ名は、構成子を持たないので素通りする。

  **`Ty.render` は全域になった。** doc コメントは型の描画から組み立てられるので、`partial` のままでは
  その文字について何も言えない。`Js.Expr.render` と同じ書き換え —— リストごとの相互再帰 —— で、
  出力は 1 バイトも動いていない。`String.intercalate` も印字器と同じ理由で落とし、パラメータの並びは
  `declSig` という素の再帰にした。**doc が `*/` を閉じないことは「`*` を 1 文字も含まない」から出る**
  —— 型名もフィールド名も識別子で、間に入るのは括弧・コロン・矢印だけ。

  `emit` の実行時検査は回帰テストとして残る
- **D-6 `runtime` を AST にする — 完了。** 手書きの 200 行あまりの JS 文字列を AST として持ち直し、
  `JsSem` がヘルパについて仮定している等式をその AST の意味論から出す。ここが TCB のいちばん厚い層。

  **`Js.Expr` は広げず、別の断片を立てる。** ヘルパはループ・可変束縛・`throw`・`typeof` を使うが、
  `Js.Expr` は**コンパイラが書く部分集合**であって、その狭さ自体が往復の主張（「読み手が受けるのは
  この処理系が書く部分集合だけ」）を支えている。構成子を足せばパーサか `Renderable` のどちらかが
  ヘルパ専用の形を抱え、`Correct` / `Renderable` / `Roundtrip` の全 `match` に届く。ヘルパは
  コンパイラが決して生成しない別の層なので、AST も意味論も別に持つ。

  **出荷テキストは変わる。** 印字器から出す以上、いまの手書きの改行と括弧は再現できない。
  1 バイトも動かさないことを条件にすると印字器が prettier の写しになるので、括弧を常に書く
  `Js.Expr.render` と同じ流儀で書き直す。振る舞いが変わっていないことを見るのは Node の差分テスト。
  **ヘルパごとの `const` はそのまま**にする —— `treeshaking.test.ts` が、使わないヘルパを
  バンドラが落とせることを見ている。

  対象は `JsSem.helper` の 32 本だけではない。`.mapJs` ほか走査 6 つと `.check` は AST の構成子で、
  その意味論が `__map` / `__ck` の振る舞いを仮定している。**`__fail` から `__ck` まで 47 本**が届く範囲。

  - **D-6-1 断片と印字器 — 完了。** `LeanTs/Helper.lean` に AST と印字器、47 本の定義。
    `Js.runtime` はそこから出る。**書き込みはローカル変数を名指しする文** —— `push` / `set` /
    フィールド代入は式ではなく文で、書き込む相手が「たった今作ったもの」であることが構文から
    出る。だから意味論は環境の再束縛だけで済み、別名付けを追う必要がない。
    **短絡演算子と `instanceof` は独自の構成子。** `bin` の綴りにすると意味論も証明も
    op の文字列で分岐することになる
  - **D-6-2 断片の意味論 — 完了。** `LeanTs/HelperSem.lean`。値は `JsValue` に `null` /
    `undefined` / 関数 / 商を足した `Val`。**JS について仮定していることは `prim` / `method` /
    `binOp` / `field` / `index` の 5 つの表だけ**で、どれもヘルパが実際に使う形でしか定義されて
    いない —— 使わない仮定を紛れ込ませられない。燃料は 1 ノード 1 で、主張は `f + k` の形。
    `#eval` で `JsSem.helper` と 70 件、`checkTy` と 23 件突き合わせて一致を確認済み
  - **D-6-3 算術 — 完了。** `LeanTs/HelperProof.lean`。`__fail` / `__i53` / `__i53div` /
    `__i53mod` / `__u32mul` / `__u32div` / `__u32mod` / `__bigdiv` / `__bigmod` / `__abs` /
    `__min` / `__max`。**主張は「そのヘルパが何を計算するか」を素の Lean で書く。**
    `JsSem.helper` と結ぶのは別の段 —— 模型は商を範囲検査するがヘルパはしないので、
    2 つが一致するのは生成コードが実際に作れる数の上だけ
  - **D-6-4 文字列 — 完了。** ループのあるものは `evalFor` の燃料を `f + xs.length + k` の形で
    置き、リストの帰納で降ろす —— 1 要素 1 燃料なので係数は 1 で足りる。環境は名前を並べて
    書き下す：`restore` は外側の束縛を名前で引き直すので、名前が具体的な列でないと簡約が止まる。
    `__trim` は前後の空白の個数で切り出すが、`strTrim` は両端から落とす。**全部が空白の文字列**
    でだけ 2 つの数え方が交差する（区間が空になるのは境界が逆転するからで、個数が揃うからではない）
    ので、その場合を先に分ける
  - **D-6-5 配列と辞書 — 完了。** `__at` / `__aslice` / `__aconcat` / `__areverse` / `__ddelete` /
    `__eq`。`__ddelete` の主張は**走らせる `foldl` そのもの**で、仮定を持たない。それが
    `JsSem` の「キーを落とす」と一致するのはキーが相異なるときだけなので、`Js.keysDistinct` を
    仮定する別の定理にした —— 相異なることは、このヘルパの性質ではなく渡される値の性質。
    **`__eq` が決めるのは `JsValue.beq` ではない。** オブジェクトは `Object.hasOwn` でキーごとに
    引くので**フィールドの順序に依らず**、辞書は連想リストが `Map` にできない重複キーを持てる。
    だから主張は `eqVal` という自前の述語に対して置き、`beq` と結ぶのは別の段。ヘルパ自身が
    再帰するので燃料は定数ではなく値の関数（`eqFuel`）で、`sizeOf` の強い帰納法で降ろす
  - **D-6-6 走査 — 完了。** `__map` … `__reduce` は「要素ごとに `ext` を 1 回呼び、最初の
    `thrown` で止まる歩き」として述べてある。`JsSem` の `evalMapJs` ほかと同じ形なので、
    2 つを並べるのは D-6-8
  - **D-6-7 入口検査 — 完了。** `__isObj` / `__hasFields` / `__has` / `__ck` が `Js.checkTy` と
    同じ述語を決める。**`__has` はフィールドを名前で引き、`checkTy` は位置で取る。** 2 つが割れるのは
    構成子のフィールド名が重複するか `tag` と衝突するときだけなので、主張は `descOk`（各構成子の
    フィールド名が相異なり、どれも `tag` でない）を仮定に置く —— `Compile.lean` が `tag` という名前を
    弾き、`Decl.lean` が相異なることを証明しているので、使う側で外れる。
    ヘルパ自身が再帰するので燃料は定数ではなく値の関数（`hasFuel`）。**`__has` は `__all` / `__find` に
    クロージャを渡す**ので、ループの主張は `.ext` ではなく任意の関数値に対して置き直し、燃料も長さでは
    なく要素ごとのコストの総和にした。
    分岐の連鎖は `simp` ではなく書き換えで降りる —— `simp` は到達不能だと決まった枝も、その枝を
    落とす `match` を簡約する前に正規化するので、9 段の `if (k === …)` を素直に歩くと 1 分岐あたり
    分単位かかる
  - **D-6-8 出荷 — 完了。** `LeanTs/HelperAgree.lean` が、模型の表 `Js.helper` の **35 行すべて**を
    書き出されるソースの計算と結ぶ（`helper_agrees`）。行ごとの主張は `HelperProof` にあるので、
    ここでやるのは値の変換（`ofJs`）と、模型とヘルパで書き方が違う 3 か所を埋めること ——
    `Math.imul` は積の 2^32 の剰余を変えないので `__u32mul` は模型の掛け算、`__i53div` はヘルパが
    しない範囲検査を模型がするので分子が Int53 に収まることを仮定、`__eq` は下記。
    **`===` は同じトップレベル関数への 2 つの参照を等しいと言う**ので、それを false にしていた
    `HelperSem.strictEq` のほうを直した —— 割れるのはオブジェクトの並びと辞書の重複キーだけになり、
    `eqShape` がその 2 つを書き下している。表の場合分けは `JsSem` 側の `helper_row` が 1 回だけ行う。
    `split` は splitter を**呼ぶ側のモジュールで**、そのモジュールが上げられないオプションのもとで
    組み立てるので、表が大きいと呼ぶ側では割れない。manifest の `Claim` は 15 本になった
- **D-7 往復を出荷する — 完了。** `file_reads_back` が manifest に入った ——「この例題のために
  書き出した `index.js` は、コンパイラが作ったモジュールに読み戻る」。README の保証の組み立てに
  「テキスト」の行が入り、AST と出荷ファイルの間が証明で埋まった。
  残る TCB は**パーサの仕様**（読み戻せることが「同じテキストである」を意味すること）**と Node**

## Step E. `.d.ts` の健全性 — 完了

**目標**: `.d.ts` が言う型と、入口の実行時検査が見る型を突き合わせる。

`.d.ts` は利用者が実際に読む唯一の型だが、そこだけ手で書いた対応の信頼で繋がっている。

**当初の目標「TS で型が通る呼び出しは弾かれない」は、成り立たないことが出荷物で確かめられた。**
`packages/verified-example` の `addMoney` を Node から呼ぶと、`.d.ts` の型を満たす次の 2 つが
`typeError` になる:

- `{ currency: "JPY", amount: 1, tag: "Money" }` —— **入口検査はフィールドを位置で見る**
  （`tag` が先頭、以降は宣言順）が、TS のオブジェクト型はキーの順序を縛らない
- `{ tag: "Money", amount: 1e300, currency: "JPY" }` —— `Int53` / `UInt32` はどちらも `number` に
  写るので、TS の型は範囲を言えない

**成り立つのは片方向** —— 入口検査を通った値は `.d.ts` の型を満たす。本体を守っているのはこちらで、
これが Step E で証明する主張になる。

- **E-1 TS 型を値の集合として定義する — 完了。** `LeanTs/Dts.lean` の `TsSat`。`tsType` と
  `declareType` が出す構文の読みで、判別可能なユニオンと型パラメータがそのまま中身になる。
  **フィールドは名前引きではなく「その名前の項目がある」で述べる** —— 引きにすると「宣言された
  フィールド名が相異なる」がすべての主張に付くが、割れるのは同じキーを二度持つオブジェクトだけで、
  それは JavaScript のオブジェクトではない
- **E-2 健全性の向き — 完了。** `checkTy_tsSat`。`Decl.checkTy_sound` が「検査を通った値は Core の
  値の符号化」まで運んでくれるので、ここでやるのは `hasTy v ty → TsSat ty (encodeValue v)`。
  manifest の `Claim` は 16 本になった
- **E-3 — 完了。** README の保証の組み立てに `.d.ts` の行が入り、上の 2 つの落とし穴も
  利用者が踏むものとして README に書いてある

**逆向きを成り立たせるかはユーザーの判断。** フィールドの並びのほうは検査を名前引きにすれば消せるが、
そのとき入口を通った値はもう `encodeValue` の像ではなくなる —— `Decl.lean` の「検査を通った引数は
Core の値の符号化である」がそこで折れるので、生成コードの側で正規順に組み直すか、対応の証明を
キーの置換を跨ぐ形に置き換えることになる。範囲のほうは TS の型では言えないので消せない。

## 順序と依存

```
A（呼び出しを断片に入れる）   断片が構文そのものになった
  ↓
B（網羅性）                  Mirrorable に残るのは outOfFuel だけになった
  ↓
D（テキスト）→ E（.d.ts）     TCB を「パーサの仕様と Node」まで縮めた
  ↓
C（燃料）                    Mirrorable そのものが主張から消えた
```

**B は A より後**（断片が閉じていないと `Mirrorable` を外しても届く範囲が変わらない）。D と E は
どれとも独立で、C を待たない。C を最後に回したのは、届く範囲が先に決まっていないと、
但し書きを外しても言えることが変わらないから。

## 守ること

- **保証を弱めて緑にしない。** 実行時検査が落ちたときに、検査対象のベクタを減らさない
- **`sorry` で塞がない**（塞げば `Axioms.lean` が落ちる）
- **manifest に定理を足したら `Axioms.lean` に行を足す**
- **帰納の軸を付け替えるコミットは、覆う範囲を動かさない。** 同じ主張が同じ形で通ることを見る
- **数字を動かしたら引用元を同じコミットで直す。** 断片が覆う形の数・関数の本数・ベクタ件数は
  `README.md` と `docs/mvp-plan.md` が引用している
- **保証の境界を動かしたら**、README の「保証の組み立て」と `docs/mvp-plan.md` の到達点を同じコミットで
  直す
- **プッシュ前にゲートを全部ローカルで通す。** 一部だけ回した結果を判断に使わない
